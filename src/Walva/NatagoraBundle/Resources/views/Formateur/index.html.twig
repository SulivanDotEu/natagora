{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Formateur list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Gsm</th>
                <th>Tel</th>
                <th>Codepostal</th>
                <th>Pays</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('formateur_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nom }}</td>
                <td>{{ entity.prenom }}</td>
                <td>{{ entity.gsm }}</td>
                <td>{{ entity.tel }}</td>
                <td>{{ entity.codePostal }}</td>
                <td>{{ entity.pays }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('formateur_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('formateur_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('formateur_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
